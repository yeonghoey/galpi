#!/usr/bin/env bash

set -euo pipefail

readonly STAGE="${1:-dev}"
readonly REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
readonly API_DIR="${REPO_DIR}/api"
readonly APP_DIR="${REPO_DIR}/app"

cd "${API_DIR}"
npm run db:dev &
npm run dev &

cd "${APP_DIR}"
npm run dev &

wait
